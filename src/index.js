import React from 'react';
import ReactDOM from 'react-dom';
import ManagementThresholdApp from "./ManagementThresholdApp";

ReactDOM.render(
  <React.StrictMode>
    <ManagementThresholdApp />
  </React.StrictMode>,
  document.getElementById('root')
);
